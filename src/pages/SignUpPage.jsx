import UserAccessForm from '../components/common/UserAccessForm';

function SignUpPage() {
  return <UserAccessForm title={'회원가입'} buttonId={'signup-button'} buttonName={'회원가입'} />;
}

export default SignUpPage;
